---
title: Data Management in Microservices
description: 'Why data is the hard part of microservices: each service owns its data, so cross-service transactions and joins disappear — and five patterns bring consistency back.'
sidebar:
  order: 1
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Context

You have split a monolith into services. Each service now runs as its own deployable, with its own team and its own release cadence. That decomposition feels clean on a whiteboard — until you reach the database. In the monolith there was one schema, one connection pool, one transaction boundary. A single `BEGIN ... COMMIT` could touch the orders table, the inventory table, and the customer ledger atomically, and a single `SELECT` with a join could stitch them back together for a report.

The moment those tables live behind separate services, that shared foundation is gone. This module is about what replaces it.

## Problem

If every service is truly independent, then no service may reach into another service's tables. But real business operations rarely respect service boundaries. Placing an order touches orders, inventory, and payments. A customer dashboard needs data scattered across half a dozen services. So the central question of this module is this:

How do you keep data consistent and queryable across services when there is no shared database, no distributed join, and no global transaction?

## Solution

The answer is not a single mechanism but a small family of cooperating patterns. The first establishes the boundary; the rest restore the capabilities the boundary takes away.

Each service keeps its own private store. Other services interact with that data only through the owning service's API or through the events it publishes.

export const overviewDiagram = `flowchart TB
  Client[Client / Gateway] --> OAPI
  Client --> IAPI
  Client --> PAPI
  subgraph Orders[Order Service]
    OAPI[API]
    ODB[(Orders DB)]
    OAPI --> ODB
  end
  subgraph Inventory[Inventory Service]
    IAPI[API]
    IDB[(Inventory DB)]
    IAPI --> IDB
  end
  subgraph Payments[Payment Service]
    PAPI[API]
    PDB[(Payments DB)]
    PAPI --> PDB
  end
  OAPI -. events .-> IAPI
  OAPI -. events .-> PAPI`;

<Mermaid code={overviewDiagram} title="Each service owns a private database; collaboration happens over APIs and events" />

The five patterns in this module fit together like this:

- **Database per Service** — every service owns a private database that only it may touch directly. This is the foundation that makes the rest necessary.
- **Saga** — keep data consistent across services without a distributed transaction, by chaining local transactions together and undoing them with compensating actions when something fails.
- **API Composition** — answer a query that spans services by calling each one and joining the results in memory.
- **CQRS** — separate the write side from one or more purpose-built read models kept in sync by events, so cross-service queries become fast local lookups.
- **Event Sourcing** — store state as an append-only log of events, replay the log to reconstruct state, and reliably publish those same events to everyone else.

## What this module covers

We start with the boundary (Database per Service), then tackle writes that span services (Saga) and reads that span services (API Composition and CQRS), and finish with a storage model that makes reliable event publishing natural (Event Sourcing). Each lesson follows the same shape: the situation that leads to the pattern, the forces in tension, what the pattern does, a diagram, runnable example code, and an honest look at what the pattern costs you.

## Related patterns

- [Database per Service](/microservices-design-from-zero-to-hero/en/data-management/database-per-service/) — start here, it is the foundation.
- [Saga](/microservices-design-from-zero-to-hero/en/data-management/saga/) — the showcase pattern for cross-service writes.

<Callout title="The mental shift">
In a monolith, consistency is a property of the database. In microservices, consistency becomes a property of your design. You trade automatic, immediate, strongly consistent guarantees for explicit, eventual, and carefully orchestrated ones — and in exchange you get services that can scale, deploy, and fail independently.
</Callout>

export const quizIndex = [
  {
    q: 'Why do cross-service transactions and joins disappear when you adopt microservices?',
    options: [
      'Because SQL is not allowed in microservices',
      'Because each service owns a private database that no other service may access directly',
      'Because microservices forbid relational databases',
      'Because every service must use a different programming language',
    ],
    answer: 1,
  },
  {
    q: 'Which pattern is the foundation that makes the other four necessary?',
    options: ['Saga', 'CQRS', 'Database per Service', 'Event Sourcing'],
    answer: 2,
  },
  {
    q: 'What is the core trade-off this module is about?',
    options: [
      'Faster code in exchange for slower databases',
      'Automatic strong consistency in exchange for explicit, often eventual, consistency you design yourself',
      'Fewer servers in exchange for more code',
      'Better security in exchange for worse performance',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="data-management/index" questions={quizIndex} />

<ProgressTracker client:visible id="data-management/index" />
