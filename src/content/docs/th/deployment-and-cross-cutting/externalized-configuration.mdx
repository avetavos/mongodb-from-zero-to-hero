---
title: Externalized Configuration
description: 'เก็บ configuration — endpoint, credential, feature flag — ไว้นอก image และ inject มันตอน runtime ผ่าน environment variable, config server หรือ secrets store เพื่อให้ artifact ที่ไม่เปลี่ยนแปลงเพียงชิ้นเดียวรันได้โดยไม่ต้องแก้ในทุกสภาพแวดล้อม'
sidebar:
  order: 6
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import { Tabs, TabItem } from '@astrojs/starlight/components';

## บริบท

ตอนนี้คุณ package แต่ละ service เป็น image ที่ไม่เปลี่ยนแปลงและเลื่อน image ตัวนั้นเป๊ะ ๆ จาก development ผ่าน staging ไปยัง production แต่ service ต้องการการตั้งค่าที่ต่างกันในแต่ละที่เหล่านั้น: database URL ที่ต่างกัน, ที่อยู่ message-broker ที่ต่างกัน, credential ที่ต่างกัน และ feature flag ที่เปิดใน staging และปิดใน production

ถ้าการตั้งค่าเหล่านั้นถูกฝังเข้าไปใน image คุณก็ไม่มี artifact เดียวอีกต่อไป — คุณมี image หนึ่งตัวต่อหนึ่งสภาพแวดล้อม แต่ละตัว build จาก code เดียวกันแต่มีค่าต่างกัน compile เข้าไป image ที่คุณทดสอบใน staging ไม่ใช่ image ที่คุณรันใน production แย่กว่านั้น การฝัง database password เข้าไปหมายความว่า secret ของคุณตอนนี้นั่งอยู่ใน image layer ใน registry อ่านได้โดยใครก็ตามที่ pull มันได้

## ปัญหา

คุณต้องการให้ image ที่ build แล้วเพียงตัวเดียวรันได้โดยไม่ต้องแก้ในทุกสภาพแวดล้อม เพื่อให้สิ่งที่คุณทดสอบเป็นสิ่งที่คุณส่งมอบเป๊ะ ๆ นั่นหมายความว่าค่าที่เฉพาะสภาพแวดล้อม — endpoint, credential, feature flag — ไม่สามารถอยู่ภายใน image ได้ พวกมันต้องมาจากภายนอก ส่งให้เมื่อ service เริ่มทำงานในที่ใดที่หนึ่ง และค่าที่อ่อนไหว เช่น password และ token ต้องถูกจัดการเป็น secret ไม่ใช่ plain text ที่นั่งอยู่ใน image หรือ repository

ดังนั้นแรงที่ขัดแย้งกันคือ: artifact หนึ่งชิ้นที่ไม่เปลี่ยนแปลงและเป็นกลางต่อสภาพแวดล้อม แต่มีการตั้งค่าที่ถูกต้อง เฉพาะสภาพแวดล้อม และปลอดภัยต่อ secret ตอน runtime

## ทางออก

รับเอา **externalized configuration** มาใช้: image บรรจุเฉพาะ code และค่าเริ่มต้นที่สมเหตุสมผล และทุกค่าที่เฉพาะสภาพแวดล้อมถูก **inject ตอน runtime** service อ่าน configuration ของมันเมื่อเริ่มทำงาน (และสำหรับบางแหล่ง ขณะที่มันรัน) แทนที่จะอ่านจากค่าคงที่ที่ compile เข้าไปในการ build

มีกลไกการ inject ที่พบบ่อยสามแบบ ซึ่งมักใช้ร่วมกัน:

- **Environment variable** — platform ตั้งค่าเช่น database URL เข้าไปในสภาพแวดล้อมของ service ตอน launch ง่าย ใช้ได้ทั่วไป และเป็นรูปแบบที่พบบ่อยที่สุด
- **Configuration server** — service ดึงการตั้งค่าจาก config service ส่วนกลางตอนเริ่มทำงาน ซึ่งเก็บ configuration ของทั้งกองทัพไว้ในที่เดียวที่ถูกควบคุมและสามารถ push การอัปเดตได้
- **Secrets store** — ค่าที่อ่อนไหวอยู่ใน secrets manager เฉพาะและถูก mount หรือดึงตอน runtime ไม่เคยถูกเขียนเข้าไปใน image หรือ source control

image ตัวเดียวกัน เริ่มใน staging ก็หยิบค่าของ staging มา เริ่มใน production ก็หยิบค่าของ production มา artifact เหมือนกัน มีเพียง configuration ที่ inject เข้ามาเท่านั้นที่ต่างกัน

export const configDiagram = `flowchart LR
  subgraph Sources[Config sources per environment]
    Env[Environment variables]
    CfgSrv[Config server]
    Sec[Secrets store]
  end
  Img[Immutable image: code + defaults] --> Svc[Service at startup]
  Env -->|inject| Svc
  CfgSrv -->|fetch| Svc
  Sec -->|mount / fetch| Svc
  Svc --> Run[Running service, configured for this environment]`;

<Mermaid code={configDiagram} title="image ที่ไม่เปลี่ยนแปลงตัวเดียว; environment variable, config server และ secrets store inject ค่าต่อสภาพแวดล้อมตอนเริ่มทำงาน" />

## ตัวอย่าง

image ไม่พ่วงค่า environment ใด ๆ การ deploy สำหรับแต่ละสภาพแวดล้อมจะ inject พวกมันเข้าไป: การตั้งค่าธรรมดามาจาก environment variable ในขณะที่ database password ถูกอ้างอิงจาก secret เพื่อให้มันไม่ปรากฏใน manifest หรือใน image

```yaml
# Production deployment — same image as staging, different injected values.
env:
  - name: SERVICE_ENV
    value: production
  - name: ORDER_DB_URL
    value: postgres://orders-db.prod.internal:5432/orders
  - name: BROKER_URL
    value: kafka://broker.prod.internal:9092
  - name: FEATURE_NEW_CHECKOUT
    value: "false"
  - name: ORDER_DB_PASSWORD
    valueFrom:
      secretRef:
        name: order-db-credentials
        key: password
```

service อ่านสิ่งเหล่านี้ตอนเริ่มทำงาน การเลื่อนขึ้น production หมายถึงการ deploy image *ตัวเดียวกัน* ด้วย manifest นี้แทนที่จะเป็นของ staging password ถูก resolve จาก secrets store ตอน launch และไม่เคยลงไปอยู่ใน image layer

## บริบทที่ได้

สิ่งที่คุณได้:

- **image เดียว ทุกสภาพแวดล้อม** artifact ที่ไม่เปลี่ยนแปลงเพียงชิ้นเดียวถูกเลื่อนจาก staging ไป production โดยไม่ต้องแก้ ดังนั้นสิ่งที่คุณทดสอบจึงเป็นสิ่งที่คุณรันเป๊ะ ๆ — ไม่มีการ build ใหม่ต่อสภาพแวดล้อม
- **secret ที่ปลอดภัยกว่า** credential อยู่ใน secrets store และถูก inject ตอน runtime ทำให้มันอยู่นอก image layer และ source control
- **การตั้งค่าจากส่วนกลางที่เปลี่ยนได้** ด้วย config server configuration ของทั้งกองทัพอยู่ในที่เดียวที่ถูกควบคุม และบางค่าสามารถเปลี่ยนได้โดยไม่ต้อง build ใหม่หรือแม้แต่ redeploy

สิ่งที่คุณต้องเสีย:

- **การจัดการ secret กลายเป็นระบบจริง** คุณต้อง operate secrets store, ควบคุมการเข้าถึงมัน และ rotate credential — การ externalize config ไม่ได้ทำให้ secret ง่าย มันทำให้มันชัดเจน
- **dependency ตอนเริ่มทำงาน** ถ้า service ดึง config จาก server หรือ secrets store แหล่งนั้นจะกลายเป็นสิ่งที่ service พึ่งพาเพื่อเริ่มทำงาน มันต้องมีความพร้อมใช้งานสูงและมี fallback ที่สมเหตุสมผล
- **การกระจัดกระจายและ drift ของ config** การตั้งค่าที่กระจายข้าม env var, config server และ secret สามารถ drift ระหว่างสภาพแวดล้อมได้ คุณต้องมีข้อตกลง, การ validate ตอนเริ่มทำงาน และแหล่งความจริงเดียวต่อแต่ละเรื่อง

## Pattern ที่เกี่ยวข้อง

- [Service per Container](/microservices-design-from-zero-to-hero/th/deployment-and-cross-cutting/service-per-container/) — externalized configuration คือสิ่งที่ทำให้ container image ที่ไม่เปลี่ยนแปลงเป็นกลางต่อสภาพแวดล้อมอย่างแท้จริง
- [Microservice Chassis](/microservices-design-from-zero-to-hero/th/deployment-and-cross-cutting/microservice-chassis/) — chassis มักเป็น component ที่อ่านและ validate configuration ที่ inject เข้ามา
- [Serverless Deployment](/microservices-design-from-zero-to-hero/th/deployment-and-cross-cutting/serverless-deployment/) — function รับการตั้งค่าในแบบเดียวกัน จากค่าที่ inject เข้ามาแทนที่จะเป็นค่าคงที่ที่ฝังไว้

<Callout title="build artifact ครั้งเดียว ตั้งค่ามันต่อที่">
วินัยพูดได้ง่าย: code และค่าเริ่มต้นเข้าไปใน image; ทุกอย่างที่เปลี่ยนระหว่างสภาพแวดล้อมมาจากภายนอกมัน ทำสิ่งนี้ให้ถูก แล้ว artifact ที่ทดสอบแล้วเพียงชิ้นเดียวจะไหลจาก staging ไป production โดยไม่ถูกแตะต้อง โดยมี secret ส่งให้อย่างปลอดภัยที่ขอบ ฝัง configuration เข้าไป แล้วคุณก็ยกคำมั่นสำคัญของ immutable deploy ทิ้งไป
</Callout>

export const quizConfig = [
  {
    q: 'อะไรคือแนวคิดหลักของ externalized configuration?',
    options: [
      'compile image คนละตัวสำหรับแต่ละสภาพแวดล้อม',
      'เก็บค่าที่เฉพาะสภาพแวดล้อมไว้นอก image และ inject มันตอน runtime เพื่อให้ image เดียวรันได้ทุกที่',
      'เก็บ configuration ทั้งหมดไว้ใน source code',
      'ปิด configuration ทั้งหมดใน production',
    ],
    answer: 1,
  },
  {
    q: 'ทำไมการฝัง database password เข้าไปใน image จึงเป็นปัญหา?',
    options: [
      'มันทำให้ image ใหญ่เกินไปจน deploy ไม่ได้',
      'secret ไปจบลงใน image layer ใน registry อ่านได้โดยใครก็ตามที่ pull มันได้ และผูก image ไว้กับสภาพแวดล้อมเดียว',
      'password ไม่สามารถเก็บเป็น text ได้เลย',
      'มันป้องกันไม่ให้ service เริ่มทำงาน',
    ],
    answer: 1,
  },
  {
    q: 'ข้อใดคือกลไกที่พบบ่อยในการ inject configuration ตอน runtime?',
    options: [
      'recompile service บนแต่ละ host',
      'environment variable, configuration server หรือ secrets store',
      'แก้ไข binary ที่กำลังรันอยู่ในที่',
      'hard-code ค่าใน Dockerfile',
    ],
    answer: 1,
  },
  {
    q: 'ข้อใดคือต้นทุนจริงของการ externalize configuration?',
    options: [
      'คุณไม่สามารถใช้ image เดียวกันข้ามสภาพแวดล้อมได้อีกต่อไป',
      'คุณต้อง operate secrets store และแหล่ง config ที่อาจจำเป็น และป้องกัน configuration drift ระหว่างสภาพแวดล้อม',
      'feature flag กลายเป็นสิ่งที่ทำไม่ได้',
      'service ไม่สามารถอ่าน configuration ตอนเริ่มทำงานได้เลย',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="deployment-and-cross-cutting/externalized-configuration" questions={quizConfig} />

<ProgressTracker client:visible id="deployment-and-cross-cutting/externalized-configuration" />
