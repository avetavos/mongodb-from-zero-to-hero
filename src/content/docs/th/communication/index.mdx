---
title: การสื่อสารใน Microservices
description: 'บริการต่าง ๆ พูดคุยกันอย่างไร: รูปแบบ synchronous กับ asynchronous, การโต้ตอบแบบ one-to-one กับ one-to-many และ pattern ที่ทำหน้าที่จัดเส้นทาง ลดการผูกมัด และค้นหาตำแหน่งของบริการ'
sidebar:
  order: 1
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import { Tabs, TabItem } from '@astrojs/starlight/components';

## บริบท

ภายใน monolith สองโมดูลทำงานร่วมกันด้วยการเรียกฟังก์ชันของกันและกัน การเรียกนั้นคือการเรียก method บน stack เดียวกัน ในโพรเซสเดียวกัน และใช้หน่วยความจำร่วมกัน มันไม่เคยล้มเหลวเพราะเครือข่าย ไม่เคย timeout ไม่เคยมาถึงซ้ำสองครั้ง และไม่เคยทำให้ข้อความสูญหาย ค่า latency วัดกันเป็นหน่วยนาโนวินาที และโหมดของความล้มเหลวก็คือโหมดความล้มเหลวธรรมดา ๆ ของโค้ดเท่านั้น

เมื่อคุณแยกโมดูลเหล่านั้นออกเป็นบริการแยกกัน ทุกการทำงานร่วมกันที่เคยเป็นการเรียกฟังก์ชันจะกลายเป็นข้อความที่ต้องข้ามเครือข่าย เครือข่ายช้ากว่าหน่วยความจำมาก มันทำแพ็กเก็ตหล่นและสลับลำดับ และบริการอีกฝั่งอาจกำลัง deploy อยู่ รับโหลดเกิน หรือหายไปเฉย ๆ ก็ได้ การสื่อสารจึงเลิกเป็นเพียงรายละเอียดเล็กน้อย และกลายเป็นหนึ่งในประเด็นสำคัญที่นิยามทั้งระบบ

## ปัญหา

การเรียกฟังก์ชันซ่อนการตัดสินใจหลายอย่างที่การเรียกบริการบังคับให้คุณต้องเลือกอย่างชัดเจน ผู้เรียกจะรอคำตอบ หรือยิงทิ้งแล้วไปต่อ? ผู้เรียกกำลังพูดกับบริการเพียงหนึ่งเดียว หรือกระจายไปยังหลายบริการ? แล้วผู้เรียกจะหาที่อยู่ของบริการที่ต้องการได้อย่างไร ในเมื่อ instance เริ่มทำงาน หยุดทำงาน และย้ายที่อยู่ตลอดเวลา? และคุณจะป้องกันไม่ให้เครือข่ายสะดุดเพียงครั้งเดียวลุกลามกลายเป็นความขัดข้องที่ล้มทั้งระบบได้อย่างไร?

ไม่มีคำตอบเดียวที่ถูกต้อง คำตอบที่ถูกขึ้นอยู่กับการโต้ตอบ และระบบจริงก็ใช้หลายรูปแบบไปพร้อมกัน โมดูลนี้จะมอบคลังคำศัพท์สำหรับรูปแบบเหล่านั้น และ pattern ที่นำมาใช้งานจริง

## ทางออก

การโต้ตอบระหว่างบริการแตกต่างกันไปตามแกนอิสระสองแกน

แกนแรกคือ **synchronous กับ asynchronous** ในการโต้ตอบแบบ synchronous ผู้เรียกส่งคำขอแล้วบล็อกรอจนกว่าจะได้รับการตอบกลับ ทั้งสองบริการถูกผูกมัดกันในเชิงเวลา เพราะทั้งคู่ต้องพร้อมใช้งานในขณะเดียวกัน ในการโต้ตอบแบบ asynchronous ผู้เรียกส่งข้อความให้ตัวกลางแล้วไปทำอย่างอื่นต่อ ผู้รับจะประมวลผลเมื่อใดก็ตามที่พร้อม ดังนั้นทั้งสองบริการไม่จำเป็นต้องทำงานพร้อมกันเลย

แกนที่สองคือ **one-to-one กับ one-to-many** การโต้ตอบแบบ one-to-one คือคำขอที่ถูกจัดการโดย instance หนึ่งของบริการหนึ่งเท่านั้น การโต้ตอบแบบ one-to-many คือข้อความที่กระจายออกไปยังผู้บริโภคทุกรายที่สนใจ ดังนั้นเหตุการณ์หนึ่งสามารถขับเคลื่อนปฏิกิริยาอิสระได้หลายอย่าง

export const overviewDiagram = `flowchart LR
  subgraph Sync[Synchronous: caller waits]
    A[Service A] -->|request| B[Service B]
    B -->|response| A
  end
  subgraph Async[Asynchronous: caller moves on]
    P[Producer] -->|message| Broker[(Broker)]
    Broker --> C1[Consumer 1]
    Broker --> C2[Consumer 2]
  end`;

<Mermaid code={overviewDiagram} title="สองรูปแบบการสื่อสาร — แบบ synchronous request/response และแบบ asynchronous messaging ผ่าน broker" />

การไขว้แกนทั้งสองทำให้ได้แผนผังง่าย ๆ ของรูปแบบการโต้ตอบ:

- **Synchronous, one-to-one** — การเรียกแบบ request/response ไปยังบริการเดียว นี่คือ **Remote Procedure Invocation (RPI)** ผ่าน REST หรือ gRPC
- **Asynchronous, one-to-one** — command ที่ส่งไปยังผู้บริโภคหนึ่งรายผ่าน queue และประมวลผลเมื่อพร้อม
- **Asynchronous, one-to-many** — event ที่เผยแพร่ไปยัง topic และส่งถึงผู้สมัครรับทุกราย ทั้งสองรูปแบบ async คือ **Messaging**

เหนือรูปแบบดิบเหล่านี้มี pattern ที่จัดระเบียบการสื่อสารสำหรับทั้งระบบ:

- **Remote Procedure Invocation** — รูปแบบที่ง่ายที่สุด: การเรียก request/response แบบมี type เข้าใจได้ง่าย แต่ผูกมัดกันในเชิงเวลา
- **Messaging** — การสื่อสารผ่าน broker ลดการผูกมัดของบริการในเชิงเวลาและรองรับการกระจาย (broadcast)
- **API Gateway** — จุดเข้าจุดเดียวที่จัดเส้นทาง รวมผลลัพธ์ และรับภาระ cross-cutting concerns ให้กับ client ภายนอก
- **Backends for Frontends** — gateway แยกต่างหากสำหรับ client แต่ละชนิด แต่ละตัวปรับแต่งเฉพาะและเป็นเจ้าของโดยทีม client
- **Service Discovery** — วิธีที่ผู้เรียกค้นหาตำแหน่งบนเครือข่ายของ instance บริการเมื่อ instance เกิดและดับ

## โมดูลนี้ครอบคลุมอะไร

เราเริ่มจากรูปแบบการโต้ตอบดิบสองแบบ — Remote Procedure Invocation และ Messaging — เพราะ pattern ที่สูงกว่าทุกตัวสร้างขึ้นจากรูปแบบเหล่านี้ จากนั้นเราดูว่า client ภายนอกเข้าถึงระบบผ่าน API Gateway อย่างไร แนวคิดนั้นพิเศษขึ้นกลายเป็น Backends for Frontends อย่างไร และสุดท้ายว่าผู้เรียกใด ๆ ค้นหา instance ที่ยังมีชีวิตอยู่ผ่าน Service Discovery อย่างไร แต่ละบทเรียนมีรูปแบบเดียวกัน: สถานการณ์ที่นำไปสู่ pattern, แรงที่ขัดแย้งกัน, สิ่งที่ pattern ทำ, แผนภาพ, ตัวอย่าง และการมองอย่างตรงไปตรงมาว่ามันมีต้นทุนอะไร

## Pattern ที่เกี่ยวข้อง

- [Remote Procedure Invocation](/microservices-design-from-zero-to-hero/th/communication/remote-procedure-invocation/) — เริ่มที่นี่ รูปแบบที่ง่ายที่สุด
- [Messaging](/microservices-design-from-zero-to-hero/th/communication/messaging/) — ทางเลือกแบบ asynchronous

<Callout title="ไม่มีรูปแบบใดดีที่สุดเพียงรูปแบบเดียว">
ระบบที่สมบูรณ์ผสมผสานรูปแบบต่าง ๆ อย่างจงใจ ผู้ใช้ที่คลิก "สั่งซื้อ" ต้องการคำตอบทันที ดังนั้นขอบของระบบจึงมักเป็น synchronous แต่งานที่คำสั่งซื้อนั้นจุดชนวน — การเรียกเก็บเงิน การจัดส่ง การแจ้งเตือน — กระจายออกไปแบบ asynchronous เพื่อให้บริการแจ้งเตือนที่ช้าไม่มีวันบล็อกการขายได้ เลือกรูปแบบตามการโต้ตอบ ไม่ใช่ตามทั้งระบบ
</Callout>

export const quizIndex = [
  {
    q: 'แกน synchronous กับ asynchronous อธิบายเรื่องอะไร?',
    options: [
      'ข้อความถูกเข้ารหัสระหว่างการส่งหรือไม่',
      'ผู้เรียกบล็อกรอการตอบกลับหรือไม่ ซึ่งผูกมัดทั้งสองบริการในเชิงเวลา',
      'บริการถูกเขียนด้วยภาษาเดียวกันหรือไม่',
      'ข้อมูลถูกเก็บในฐานข้อมูลเดียวกันหรือไม่',
    ],
    answer: 1,
  },
  {
    q: 'รูปแบบการโต้ตอบแบบใดเป็น one-to-many?',
    options: [
      'คำขอ REST ไปยังบริการเดียว',
      'การเรียก gRPC ที่คืนค่ามาหนึ่งการตอบกลับ',
      'event ที่เผยแพร่ไปยัง topic และส่งถึงผู้สมัครรับทุกราย',
      'command ที่ส่งไปยังผู้บริโภคใน queue หนึ่งราย',
    ],
    answer: 2,
  },
  {
    q: 'เหตุใดการเรียกบริการจึงแตกต่างจากการเรียกฟังก์ชันโดยพื้นฐาน?',
    options: [
      'เพราะเขียนได้ช้ากว่าเสมอ',
      'เพราะมันข้ามเครือข่ายที่อาจช้า ทำข้อความหล่น และพบว่าอีกฝั่งไม่พร้อมใช้งาน',
      'เพราะมันต้องใช้ภาษาโปรแกรมที่ต่างกัน',
      'เพราะฟังก์ชันไม่สามารถคืนค่าข้ามบริการได้',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="communication/index" questions={quizIndex} />

<ProgressTracker client:visible id="communication/index" />
